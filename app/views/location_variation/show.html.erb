<h1>Books for <%= @location.name %></h1>

<table width='100%'>
  <tr>
  </tr>

<% @books.each do |book| %>
  <tr>
    <td style="padding:5px;"><%= link_to book.title, book_path(book) %></td>
  </tr>
<% end %>
</table>

<br />
