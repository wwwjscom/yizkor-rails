<% form_for(@alternate_title, :url => book_alternate_titles_path(@book)) do |f| %>
  <%= f.error_messages %>

  <fieldset>
    <legend>New Alternate Title</legend>
    <p>
    <%= f.label :title %>
    <%= f.text_field :title %>
    </p>
    <p>
    <%= f.submit "Create" %>
    </p>
  </fieldset>
<% end %>
